# tests/api/test_auth.py — README

Overview
- Purpose: Simple test verifying the `AuthAPI.get_token()` helper returns a token.

Call mapping:
- `AuthAPI.get_token()` → `api/auth.py`

Behavior summary
- Calls `AuthAPI.get_token()`, prints and asserts it is not `None`.

---
Generated README for `tests/api/test_auth.py`.