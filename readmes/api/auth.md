# api/auth.py — README

Overview
- Purpose: Authentication helper for API tests. Currently provides a placeholder token to simplify examples.

Class: `AuthAPI`
- `get_token()` (static)
  - Returns a string token (currently `"dummy_token_123"`).

Notes
- Replace with real token retrieval logic (login endpoint) if integrating with a secured API.

---
Generated README for `api/auth.py`.